---
type: flashcard
---
What is the primary function of a web browser's rendering engine?
===
The rendering engine is responsible for parsing web documents (like HTML and XML) and CSS, and displaying the formatted content on the screen.

---
type: multiple-choice
answer: C
options:
  - A) To execute JavaScript code
  - B) To handle HTTP requests
  - C) To interpret and display HTML and CSS
  - D) To store user session data
---
What is the main purpose of the 'Box Model' in CSS?

---
type: true-false
answer: True
---
In JavaScript, `null` is an object.

---
type: fill-in-the-blank
blanks:
  - state
  - props
---
In React, ___ is mutable and managed within a component, while ___ are immutable and passed down from parent components.

---
type: matching
pairs:
  'useState': 'Manages state in a functional component'
  'useEffect': 'Performs side effects in a functional component'
  'useContext': 'Accesses a context''s value'
  'useReducer': 'An alternative to useState for complex state logic'
---
Match the React hook to its primary purpose.

---
type: ordering
items:
  - Mounting
  - Updating
  - Unmounting
---
Arrange the main phases of a React component's lifecycle in order.

---
type: multi-select
answers:
  - B
  - D
options:
  - A) `var`
  - B) `let`
  - C) `const` (if re-assignment is considered)
  - D) `function` (hoisted)
---
Which of the following are hoisted in JavaScript?

---
type: flashcard
---
What is the quadratic formula?
===
The solution to the quadratic equation $ax^2 + bx + c = 0$ is given by the formula: $$x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}$$

---
type: multiple-choice
answer: D
options:
  - A) $O(n)$
  - B) $O(\log n)$
  - C) $O(n^2)$
  - D) $O(1)$
---
What is the time complexity of accessing an element in a hash map by its key, on average?

---
type: true-false
answer: False
---
A `Promise` in JavaScript can transition from the 'fulfilled' state back to the 'pending' state.

---
type: fill-in-the-blank
blanks:
  - git rebase -i HEAD~3
---
The command to interactively squash the last 3 commits in Git is ___.

---
type: matching
pairs:
  'git clone': 'Create a local copy of a remote repository'
  'git pull': 'Fetch from and integrate with another repository or a local branch'
  'git push': 'Update remote refs along with associated objects'
  'git fetch': 'Download objects and refs from another repository'
---
Match the Git command to its description.

---
type: ordering
items:
  - TCP Handshake (SYN, SYN-ACK, ACK)
  - HTTP Request
  - HTTP Response
  - TCP Teardown (FIN, ACK, FIN, ACK)
---
Arrange the steps of a simple HTTP web request in the correct sequence.

---
type: multi-select
answers:
  - A
  - C
  - E
options:
  - A) `Array.prototype.map`
  - B) `Math.random`
  - C) `Array.prototype.filter`
  - D) `console.log`
  - E) `Array.prototype.reduce`
---
Which of the following are considered "pure" functions, assuming they are called with pure callback functions?

---
type: flashcard
---
Explain the difference between `==` and `===` in JavaScript.
===
The `==` operator performs type coercion before comparison, meaning it will try to convert the operands to the same type.

The `===` operator (strict equality) does not perform type coercion and returns `true` only if both the value and the type are the same.
Example: `5 == '5'` is true, but `5 === '5'` is false.

---
type: multiple-choice
answer: A
options:
  - A) `display: flex;`
  - B) `display: block;`
  - C) `display: inline;`
  - D) `display: grid;`
---
Which CSS property is fundamental to creating a flexbox layout?

---
type: true-false
answer: True
---
In CSS, the `rem` unit is relative to the font-size of the root (`html`) element.

---
type: fill-in-the-blank
blanks:
  - virtual DOM
---
React uses a ___ to efficiently update the browser's DOM by creating an in-memory representation of the UI, computing the difference, and then updating the actual DOM only where necessary.

---
type: matching
pairs:
  '404': 'Not Found'
  '200': 'OK'
  '500': 'Internal Server Error'
  '301': 'Moved Permanently'
  '403': 'Forbidden'
---
Match the HTTP status code to its meaning.

---
type: ordering
items:
  - `npm install`
  - `npm run build`
  - `npm run deploy`
---
What is the typical sequence of commands to deploy a frontend application from a Git repository?
